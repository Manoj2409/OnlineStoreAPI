[pytest]
markers =
    smoke: quick high-priority API checks
    sanity: focused checks for core behavior
    regression: broader API regression coverage
    dependency: declare dependencies between tests
